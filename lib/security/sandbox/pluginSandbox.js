export function runPluginSandbox(manifest = {}, code = "") {
  // Placeholder: real impl would run in isolated VM with limited APIs.
  return {
    ok: true,
    manifest,
    note: "Sandbox placeholder (no execution).",
  };
}
