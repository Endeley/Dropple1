export async function generateMotionInstructions({ context, instruction }) {
  // Placeholder: route to AI model for motion instructions.
  return {
    note: "Motion LLM not implemented; returning empty instructions.",
    actions: [],
  };
}
